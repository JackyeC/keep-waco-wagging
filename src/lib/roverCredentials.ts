import { cityConfig } from "@/lib/site";

/** Approved public Rover credential line — always label the platform. */
export const roverCredentialsLine = `${cityConfig.rover.rating} on Rover · ${cityConfig.rover.reviewCount} Rover reviews · Rover Star Sitter`;
